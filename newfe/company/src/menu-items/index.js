import profile from "./profile";
import jobs from "./jobs";
import interview from "./interview";

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [profile, jobs, interview]
};

export default menuItems;
